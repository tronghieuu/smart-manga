
# Code generation
gen: 
  # Author
  author: Trong Hieu
  # The default generated package path system needs to be changed to its own module name, such as system monitor tool
  packageName: com.smanga.system
  # Automatically remove the table prefix, the default is false
  autoRemovePre: false
  # Table prefix (the generated class name will not contain the table prefix, multiple separated by commas)
  tablePrefix: sys_